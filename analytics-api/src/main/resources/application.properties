server.port=8083
spring.application.name=analytics-api

# ClickHouse
# Connect to 'default' DB — all queries use fully qualified analytics.table_name
app.clickhouse.url=jdbc:clickhouse://localhost:8123/default?compress=0
app.clickhouse.username=default
app.clickhouse.password=

# Google OAuth2 — same as write-api
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=https://www.googleapis.com/oauth2/v3/certs
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://accounts.google.com
